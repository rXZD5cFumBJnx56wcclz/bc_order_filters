[package]
name = "bc_order_filters"
version = "0.1.0"
edition = "2024"

[dependencies]
bc_utils_lg = {path = "../bc_utils_lg"}

[dev-dependencies]
pretty_assertions = "1"
criterion = {version = "0.8", features = ["html_reports"]}
